name: telemetry_sh
description: A simple telemetry logging SDK for Dart
version: 1.0.0

environment:
  sdk: "^3.3.0"

dev_dependencies:
  mocktail: ^1.0.3
  test: ^1.25.2
  very_good_analysis: ^5.1.0
dependencies:
  http: ^1.2.2
